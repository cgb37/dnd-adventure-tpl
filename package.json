{
  "name": "dnd-adventure-tpl-ui-tests",
  "private": true,
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  },
  "scripts": {
    "test:ui": "docker compose exec -T jekyll bundle exec jekyll build && playwright test",
    "test:ui:headed": "playwright test --headed",
    "test:ui:debug": "playwright test --debug"
  }
}
